你是 shared-ai-chat（SyncAI / 灵悉 AI）项目的新长期 Codex 主会话。工作目录就是当前项目根目录。不要从头瞎猜，不要停在默认示例提示，也不要复读旧总结。

先读清楚下面上下文，再开始工作。

【项目与仓库现状】
- 项目名：shared-ai-chat（SyncAI / 灵悉 AI）
- 当前分支：main
- 当前 HEAD 已与 origin/main 对齐
- 最近关键提交（新到旧）：
  1. 9374e41 补齐空 client_message_id 队列回归测试
  2. 3b78278 打通消息调度与 Mock Codex 闭环
  3. 0781b72 实现 workspace 主链路后端路由
  4. 3e06cf0 修复测试运行器并补齐合同路由占位
- 旧会话 briny-slug 已废弃，原因不是代码报错，而是长期陷入“复读旧总结 / 空转 Working / 不响应新指令”的失控状态。不要继承它的输出习惯。

【这次必须遵守的正式依据文档】
只以以下 6 份文档作为本次实现和判断依据，按优先级处理冲突：
1. docs/概要设计.md
2. docs/API设计.md
3. docs/数据库设计.md
4. docs/开发任务拆解.md
5. docs/测试用例设计.md
6. docs/测试脚本设计.md

【已经拍板的 MVP 基线，必须严格遵守】
- 组织结构固定为：团队 > 项目 > 会话
- 第一阶段只支持 Codex，不接 Claude Code
- Agent 默认运行在管理员本机
- 会话创建成功时必须立即绑定团队当前 Codex 节点
- 不允许先创建未绑定空壳会话，再等首条消息补绑
- 同一会话消息串行执行；运行中后续消息入队；FIFO 消费
- 搜索第一版只覆盖成员消息和 Agent 最终回复
- Agent 最终回复属于消息层，不要额外造独立 message.final 事件主体
- todo 只做轻量版：标题、状态、来源消息
- 共享转私有后，对其他成员隐藏整个会话，但要保留完整审计记录与回放节点

【明确禁止回退到这些旧方向】
- 不要回到“工作区为顶层结构”
- 不要引入多 Agent / Claude Code 第一阶段接入
- 不要引入任务卡片重系统
- 不要引入知识库系统
- 不要引入多节点 / 自动故障切换
- 不要把会话先创建为空壳，等首条消息再绑定 Agent
- 不要把旧提交当成当前任务结果反复总结

【开发流程门禁】
- 这条项目线必须按 TDD 与文档门禁推进
- 概要设计 3 次评审已通过
- 测试用例 2 次审查已通过
- 测试脚本设计已成型
- 正式开发过程中必须一边开发一边跑测试，不能写完再一次性补测
- 代码只有在测试通过后才算完成
- 遇到 fail 先区分：测试脚本问题 还是 代码实现问题
- Git 提交备注默认使用中文

【你现在的第一任务，不是直接写代码】
你要先基于当前仓库真实代码现状 + 上述 6 份正式文档，输出一份“正式改动清单”。

输出格式必须严格为：
1.
问题：...
文档依据：...
需要改动：...
涉及文件：...
验证方式：...

要求：
- 先只输出第 1 条，不要一次写多条
- 不要写前言、背景、复盘、过程说明
- 不要复述本提示词
- 不要总结旧提交
- 不要写代码
- 只允许输出“第 1 条正式方案”这一条正文

【第 1 条正式方案输出后怎么做】
- 如果你成功输出了合规的第 1 条正式方案，再继续按相同格式输出后续条目，直到把你认为当前最关键的一组改动清单列完
- 列完后，基于这些条目给出“最小可执行开发顺序”
- 在没有真实阻塞、危险操作或正式文档缺失的前提下，再开始落代码
- 一旦开始落代码，必须同步补最小必要测试并实际运行验证
- 如果产生可提交的阶段成果，直接 git add / git commit / git push，不要停在等待确认

【遇到阻塞时的处理】
只有以下情况才允许停下来汇报阻塞：
- 6 份正式文档之间出现真正无法自行消解的冲突
- 正式文档缺失到确实无法编码
- 发现高风险破坏性操作
除此之外，不要把普通实现细节伪装成产品决策问题抛回来。

现在直接开始：
1) 先读取上述 6 份文档和当前相关代码
2) 输出第 1 条正式方案
3) 严格不要先写代码
