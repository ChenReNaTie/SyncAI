# 2026-04-17

- 补掉了 `docs/概要设计第2次评审.md` 剩余的最后一个硬门禁：`server/migrations/0001_initial_schema.sql` 中 `sessions.bound_agent_node_id` 与 `sessions.bound_agent_session_ref` 已收紧为 `NOT NULL`，与“创建成功即必须绑定 Codex”正式口径一致。
- 复核了 `docs/概要设计.md`、`docs/API设计.md`、`docs/数据库设计.md`、`docs/开发任务拆解.md` 与迁移口径，当前无需继续同步修改。
- 已把 `docs/概要设计第2次评审.md` 的结论更新为：A 组硬门禁全部关闭，现在允许进入概要设计第 3 次评审。
- 已进入概要设计第 3 次评审准备与执行阶段；补做了 `docs/概要设计.md` 的最小同步，修正回放接口与可见性切换接口表述，使其与当前已拍板规则一致。
- 新增 `docs/概要设计第3次评审.md`，结论为：第 3 次评审输入齐备，未发现新的概要设计级硬门禁，概要设计阶段评审通过，可进入下一阶段的测试设计与审查。
- 已进入测试设计与审查阶段：新增 `docs/测试设计.md` 作为测试设计总文档，明确测试目标、分层、核心业务链路、风险覆盖点、通过标准和阶段完成态。
- 已重写 `docs/测试用例设计.md`，按模块整理 35 个核心 case，补入“创建即绑定失败”“回放包含可见性切换”“不生成 `message.final` 事件”“共享转私有写入回放时间线”等新边界。
- 当前结论是：测试设计已成型，可进入测试用例第 1 次审查；尚未进入测试脚本审查，也未进入正式编码实现。
- 基于仓库现状复核后，确认 `docs/概要设计第3次评审.md` 已作为第 3 次概要设计评审的正式收口文档存在；当前仍待补齐的不是门禁本身，而是更早第 1 次评审的显式历史留痕证据。
- 已完成 `docs/测试用例第1次审查.md`：本轮未发现需要回退到概要设计阶段的问题，但识别出两个必须在第 2 次审查前补齐的缺口——“私有 -> 共享”转换测试用例、消息发送幂等 / `MESSAGE_DUPLICATED` 测试用例；另有审计日志读取路径与 cursor 分页两个建议补齐点。
- 已读取并对齐 `docs/测试用例设计.md`、`docs/测试用例第1次审查.md`、`docs/测试用例第2次审查.md`、`docs/测试脚本设计.md`，确认测试用例基线已扩展为 40 条，且测试用例第 2 次审查已通过。
- 已更新 `docs/测试脚本设计.md` 到 v2.1：补入回归层脚本、回归层脚本组、失败后定位与回放方法、回归层执行顺序与目录骨架规则，使其不再只停留在分层说明。
- 已新增 `docs/测试脚本第1次审查.md`：本轮判定测试脚本第 1 次审查通过，但要求在第 2 次审查前继续细化“回归层入口命名/聚合规则”与“真实 Codex 人工补充验收的发布阻塞规则”。
- 已将 `docs/测试用例设计.md` 更新到 v1.2：补齐 `TC-B07`（消息幂等 / `MESSAGE_DUPLICATED`）、`TC-A07` / `TC-D05`（列表与搜索分页 cursor 合同）、`TC-F06` / `TC-F07`（私有 -> 共享方向），并把审计日志接口读取路径写入 `TC-F02` / `TC-F07`。
- 已重写 `docs/测试脚本设计.md`，以最新 40 条测试用例为基线重新明确脚本分层、执行顺序、覆盖关系、夹具 / 数据准备和通过标准。
- 已新增 `docs/测试脚本第1次审查.md`，结论为：测试脚本设计第 1 次审查通过，可进入测试脚本第 2 次审查；当前仍不能进入编码。
