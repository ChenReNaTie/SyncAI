# SyncAI / 灵悉 AI - 测试脚本第1次审查

**审查批次：** 测试脚本第 1 次审查  
**审查日期：** 2026-04-17  
**审查范围：** 仅审查 `docs/测试脚本设计.md` 是否已按最新 40 条测试用例完成脚本分层、覆盖映射、执行顺序、夹具准备、失败定位与回放方案；不进入编码实现，不进入真实脚本编写。  
**审查基线：** `docs/产品需求文档.md`、`docs/概要设计.md`、`docs/API设计.md`、`docs/数据库设计.md`、`docs/测试设计.md`、`docs/测试用例设计.md`、`docs/测试用例第2次审查.md`、`docs/Codex协作约束.md`

---

## 1. 本轮审查结论

- **测试脚本第 1 次审查：通过。**
- **判定方式：带修改建议通过，不阻塞进入测试脚本第 2 次审查前的细化修订。**
- 当前 `docs/测试脚本设计.md` 已满足本轮审查的核心门槛：
  - 已明确脚本分层
  - 已明确每层主要覆盖的测试用例
  - 已明确完整执行顺序
  - 已明确夹具、种子数据、模拟边界与前置准备
  - 已明确失败后的定位与回放方法

---

## 2. 本轮通过依据

### 2.1 40 条测试用例已被纳入脚本设计输入

- `docs/测试脚本设计.md` 已明确以 `docs/测试用例设计.md` 与 `docs/测试用例第2次审查.md` 为输入基线。
- 当前脚本组覆盖表已经覆盖会话创建、调度排队、回放、搜索、todo、可见性切换、异常恢复与回归编排，不再停留在旧版 35 条用例口径。

### 2.2 脚本分层已经成型

- 当前已形成以下层级：
  - 合同层
  - 集成层
  - 端到端层
  - 回归层
  - 人工补充验收
- 该结构可以同时承接接口合同、状态联动、协作体验与真实 Codex 补充验收，满足当前 MVP 测试设计边界。

### 2.3 执行顺序已经可落地

- 文档已给出单轮完整回归顺序：
  - `env_smoke`
  - 合同层 P0
  - 集成层 P0
  - 端到端高价值协作集
  - 回归层编排
  - 真实 Codex 人工补充验收
- 同时补充了：
  - 提交前最小回归集
  - 预发布前回归集

### 2.4 夹具、种子数据与模拟边界已经明确

- 已明确环境夹具、账号夹具、组织夹具、会话夹具、消息 / 事件夹具、审计与时间控制夹具。
- 已明确 `mock` / `real` 双模式 Adapter 边界，以及不同层级对夹具和清理策略的要求。

### 2.5 失败定位与回放路径已经补齐

- 文档已明确：
  - 失败定位顺序
  - 失败分类
  - 失败回放材料
  - 合同层 / 集成层 / 端到端 / 回归层各自的重放方法
  - 根因归档要求

---

## 3. 本轮发现的缺口

### A-1 回归层脚本组仍需在第 2 次审查前进一步细化到“脚本入口级”

- **严重度：中**
- **问题说明：**
  - 当前已定义 `regression_commit_gate` 与 `regression_pre_release` 两类回归层脚本组。
  - 但还没有把它们进一步细化为未来实际脚本入口约定，例如：
    - 是否按 `contracts + integration` 自动聚合
    - 是否单独维护回归清单文件
    - 回归层失败时如何回指到底层脚本组名称
- **当前判定：**
  - 不阻塞第 1 次审查通过
  - 建议在第 2 次审查前补清“回归层编排入口命名规则”

### A-2 人工补充验收与自动化回归的衔接还可再明确

- **严重度：中**
- **问题说明：**
  - 当前文档已保留 `manual_real_codex_acceptance`。
  - 但尚可进一步写清：
    - 它与 `regression_pre_release` 的先后依赖
    - 它失败时是否阻塞发布
    - 哪些结果必须回写到自动化回归的缺口清单
- **当前判定：**
  - 不阻塞本轮通过
  - 建议在第 2 次审查前补成“发布前验收准入规则”

---

## 4. 修改建议

- 建议 1：在 `docs/测试脚本设计.md` 中为回归层新增“入口命名与聚合规则”小节，明确回归清单如何对应到底层脚本组。
- 建议 2：在人工补充验收部分新增“发布阻塞规则”说明，明确真实 Codex 验收失败时的处理方式。
- 建议 3：在推荐目录骨架中，后续可考虑为 `regression/` 增加 `suites/` 或清单文件说明，便于未来落真实脚本时直接套用。

---

## 5. 本轮审查判定

- **是否通过：通过。**
- **是否存在阻塞当前阶段推进的缺口：否。**
- **是否需要用户拍板：否，暂无需用户拍板。**
- **下一步：**
  - 基于本轮建议修订 `docs/测试脚本设计.md`
  - 进入测试脚本第 2 次审查
  - 测试脚本阶段门禁完成后，再进入 TDD 开发与验证

---

## 6. 本轮摘要

- 当前测试脚本设计已经从“概念分层”推进到“可执行方案分层”。
- 40 条测试用例已有明确脚本承接关系，且失败后定位 / 回放路径已被写成正式规则。
- 本轮仍有两项中等级别细化建议，但都不构成阻塞。
- **结论：测试脚本第 1 次审查通过，可继续修订后进入第 2 次审查。**
