你现在接手 SyncAI 项目的持续推进任务。工作目录是当前目录 `C:\Users\Administrator\.openclaw\workspace\shared-ai-chat`。

这是一个持续项目，请按“一个项目一个 Codex 主会话”的方式工作。当前目标不是停留在需求讨论，而是持续往前推进。

当前已确认的基线：
1. `docs/产品需求文档.md`
2. `docs/MVP架构设计.md`
3. `docs/Agent接入设计.md`
这三份文档已作为当前开发基线。

已拍板规则（不要重新讨论，也不要改写成别的口径）：
- 搜索第一版 = 成员消息 + Agent 最终回复
- 历史回放第一版 = 消息 + 状态变化 + 命令过程摘要
- 第一版默认单管理员执行节点，即整个团队先共享 1 个管理员本机上的 Codex 执行环境
- 当前会话运行中时，后续成员消息直接入队，前端显示“排队中”
- 共享转私有后，对其他成员隐藏整个会话，但平台保留共享起止时间 + 当时可见成员范围的审计记录
- 第一阶段主接入只支持 Codex，Claude Code 后续再补
- MVP 结构 = 团队 > 项目 > 会话
- Todo 只做轻量 todo：手动从消息提取，字段仅标题+状态，状态仅待办/已完成，保留来源消息跳转，展示在会话侧边栏

现在按以下顺序持续推进：

阶段 A：概要设计
- 基于现有 3 份基线文档，补出一份“概要设计”主文档
- 概要设计重点覆盖：系统分层、核心模块、关键数据流、关键状态流、模块边界、接口边界、关键数据模型、部署形态、测试策略入口
- 概要设计不要漂，不要写成宣传稿，要能直接支撑后续测试设计和开发拆解
- 如有必要，可以同步修正现有文档中的轻微不一致，但不要引入新的未拍板产品能力

阶段 B：测试设计
- 基于概要设计，输出测试用例文档
- 测试用例至少覆盖：共享/私有会话、排队中、消息搜索、历史回放、todo 提取、共享转私有审计、单管理员节点约束、Codex 接入主链路
- 再基于测试用例，设计测试脚本方案
- 如果项目里还没有代码骨架，可以先输出测试脚本设计文档和脚本目录规划；如果已有合适骨架，则直接落测试脚本文件

阶段 C：开发落地准备
- 基于概要设计和测试设计，拆第一批开发任务
- 第一批开发任务要求可执行、可排序、可直接进入编码
- 如已经具备合适代码骨架，再推进到代码实现；如暂不具备，则先把任务清单和目录骨架补齐

执行原则：
- 你可以自主持续推进，不需要每一步都停下来问
- 只有遇到真正需要用户拍板的设计分歧，才汇报阻塞项
- 不要重复做已经拍板的需求讨论
- 不要写空泛总结，要优先产出真实文档、真实文件、真实改动
- 优先中文文档命名

输出要求：
- 直接修改或新增文件
- 结束时按文件分组汇报：
  1) 新增/修改了哪些文件
  2) 每个文件产出了什么
  3) 当前是否存在必须由用户拍板的阻塞项
  4) 如果没有阻塞项，下一步准备继续推进什么
